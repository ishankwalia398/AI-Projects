{
  "version": 2,
  "buildCommand": "cd qa-portfolio && npm install && npm run build",
  "outputDirectory": "qa-portfolio/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
